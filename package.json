{
  "name": "deepts",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "data": "bash scripts/download-mnist.sh",
    "dev": "tsx watch src/index.ts",
    "build": "tsc && cp -r src/weights dist/weights && (cp -r src/data dist/data 2>/dev/null || true)",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
